name: Forgejo

on:
    push:
        branches:
            - main
        paths:
            - "images/forgejo/**"
            - ".github/workflows/forgejo.yaml"
            - "!images/forgejo/README.md"
    workflow_dispatch:

jobs:
    build-rst:
        runs-on: ubuntu-latest
        permissions:
            contents: read
            security-events: write
            id-token: write

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Get upstream version
              id: get_version
              run: |
                  VERSION=$(grep '^FROM ' images/forgejo/Dockerfile | awk -F':' '{print $2}')
                  echo "UPSTREAM_VERSION=$VERSION" >> $GITHUB_ENV

            - name: Extract Docker metadata
              id: meta
              uses: docker/metadata-action@v5
              with:
                  images: kethernoir/forgejo-custom
                  flavor: |
                      latest=auto
                      suffix=-rst,onlatest=true
                  tags: |
                      type=semver,pattern={{version}},value=${{ env.UPSTREAM_VERSION }}
                      type=semver,pattern={{major}}.{{minor}},value=${{ env.UPSTREAM_VERSION }}
                      type=semver,pattern={{major}},value=${{ env.UPSTREAM_VERSION }}

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v3

            - name: Install Cosign
              uses: sigstore/cosign-installer@v3

            # Login to your registry (e.g., Docker Hub or GitHub Container Registry)
            - name: Login to Docker Hub
              uses: docker/login-action@v3
              with:
                  username: ${{ secrets.DOCKERHUB_USERNAME }}
                  password: ${{ secrets.DOCKERHUB_TOKEN }}

            - name: Build Docker image (local, for scanning)
              uses: docker/build-push-action@v5
              with:
                  context: images/forgejo
                  file: images/forgejo/Dockerfile
                  push: false
                  load: true
                  tags: ${{ steps.meta.outputs.tags }}
                  labels: ${{ steps.meta.outputs.labels }}

            - name: Scan image with Trivy
              uses: aquasecurity/trivy-action@master
              with:
                  image-ref: kethernoir/forgejo-custom:${{ env.UPSTREAM_VERSION }}-rst
                  format: sarif
                  output: trivy-rst.sarif
                  severity: CRITICAL,HIGH
                  exit-code: "0"

            - name: Upload Trivy scan results
              if: always()
              uses: github/codeql-action/upload-sarif@v3
              with:
                  sarif_file: trivy-rst.sarif
                  category: trivy-rst

            - name: Push Docker image
              id: push
              uses: docker/build-push-action@v5
              with:
                  context: images/forgejo
                  file: images/forgejo/Dockerfile
                  push: true
                  tags: ${{ steps.meta.outputs.tags }}
                  labels: ${{ steps.meta.outputs.labels }}

            - name: Sign image with Cosign
              env:
                  COSIGN_EXPERIMENTAL: "true"
              run: |
                  echo "${{ steps.meta.outputs.tags }}" | while read tag; do
                    cosign sign --yes "${tag}@${{ steps.push.outputs.digest }}"
                  done
