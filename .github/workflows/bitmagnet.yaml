name: Bitmagnet

on:
    push:
        branches:
            - main
        paths:
            - "images/bitmagnet/**"
            - ".github/workflows/bitmagnet.yaml"
            - "!images/bitmagnet/README.md"
    workflow_dispatch:

jobs:
    build-bitmagnet:
        runs-on: ubuntu-latest
        permissions:
            contents: read
            security-events: write
            id-token: write

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Get upstream version
              id: get_version
              run: |
                  VERSION=$(grep '^ARG BITMAGNET_VERSION=' images/bitmagnet/Dockerfile | cut -d'=' -f2)
                  echo "UPSTREAM_VERSION=$VERSION" >> $GITHUB_ENV

            - name: Extract Docker metadata
              id: meta
              uses: docker/metadata-action@v5
              with:
                  images: kethernoir/bitmagnet-custom
                  flavor: |
                      latest=false
                  tags: |
                      type=raw,value=${{ env.UPSTREAM_VERSION }}-envconsul
                      type=raw,value=latest-envconsul

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v3

            - name: Install Cosign
              uses: sigstore/cosign-installer@v3

            - name: Login to Docker Hub
              uses: docker/login-action@v3
              with:
                  username: ${{ secrets.DOCKERHUB_USERNAME }}
                  password: ${{ secrets.DOCKERHUB_TOKEN }}

            - name: Build Docker image for scanning
              uses: docker/build-push-action@v5
              with:
                  context: images/bitmagnet
                  file: images/bitmagnet/Dockerfile
                  builder: ${{ steps.buildx.outputs.name }}
                  push: false
                  load: true
                  tags: ${{ steps.meta.outputs.tags }}
                  labels: ${{ steps.meta.outputs.labels }}

            - name: Scan image with Trivy
              uses: aquasecurity/trivy-action@master
              with:
                  image-ref: kethernoir/bitmagnet-custom:${{ env.UPSTREAM_VERSION }}-envconsul
                  format: sarif
                  output: trivy-envconsul.sarif
                  severity: CRITICAL,HIGH
                  exit-code: "0"

            - name: Upload Trivy scan results
              if: always()
              uses: github/codeql-action/upload-sarif@v3
              with:
                  sarif_file: trivy-envconsul.sarif
                  category: trivy-envconsul

            - name: Push Docker image
              id: push
              uses: docker/build-push-action@v5
              with:
                  context: images/bitmagnet
                  file: images/bitmagnet/Dockerfile
                  builder: ${{ steps.buildx.outputs.name }}
                  push: true
                  tags: ${{ steps.meta.outputs.tags }}
                  labels: ${{ steps.meta.outputs.labels }}

            - name: Sign image with Cosign
              env:
                  COSIGN_EXPERIMENTAL: "true"
              run: |
                  echo "${{ steps.meta.outputs.tags }}" | while read tag; do
                    cosign sign --yes "${tag}@${{ steps.push.outputs.digest }}"
                  done
